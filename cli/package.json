{
  "name": "@moda-design/cli-src",
  "private": true,
  "version": "0.7.1",
  "type": "module",
  "description": "Source for the moda CLI. Compiled with `bun build --compile` (scripts/build.ts).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
